# Production requirements — core web + API only
# Heavy ML packages (torch, transformers, ultralytics, paddleocr, speechbrain)
# are NOT included here — they load lazily and gracefully fail when absent.
# The AI inference endpoints return 503 with a clear message if the model isn't installed.
# Install them separately on a GPU-enabled server when ready for full AI features.

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
alembic==1.13.2
greenlet==3.0.3

# Data validation
pydantic==2.8.2
pydantic-settings==2.3.4
email-validator==2.2.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# Async HTTP
httpx==0.27.0

# Task queue
celery[redis]==5.4.0
redis==5.0.7

# Notifications
aiosmtplib==3.0.1
jinja2==3.1.4
firebase-admin==6.5.0

# Core computer vision (lightweight — no GPU needed)
opencv-python-headless==4.10.0.84
numpy==1.26.4
scikit-learn==1.5.1
supervision==0.22.0
pillow==10.4.0
onnxruntime==1.18.1

# SOAR AI (API-based, no local model download)
langgraph>=0.2.0
langchain-core>=0.2.0
langchain-anthropic>=0.1.0

# Utilities
python-dateutil==2.9.0
pytz==2024.1
python-magic==0.4.27

# Elasticsearch client
elasticsearch[async]==8.13.0
